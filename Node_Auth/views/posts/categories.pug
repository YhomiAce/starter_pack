extends ../layouts

block content
  
  .row.mx-auto
    .col-md-6
    
      .card
        .card-header.bg-grey
          h3 Add Category
        .card-body
          form(method="post" action="/posts/category")
            .form-group
              input(type="text" name="category" class="form-control")
            button(type="submit" class="btn btn-primary") Add category
